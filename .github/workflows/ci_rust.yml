name: CI - Rust

on:
  pull_request:
    paths:
      - 'components/**'
      - 'src/**'
      - 'build.rs'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'Cross.toml'
      - 'documentation/docs/examples/**/*.yml'
      - 'documentation/docs/getting_started/**/*.yml'
      - '.github/workflows/ci_rust.yml'
      - '.github/workflows/build-and-test-template.yml'
    branches-ignore:
      - release
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            runs-on: ubuntu-24.04
            fileending: ''

          - target: arm-unknown-linux-musleabi
            runs-on: ubuntu-24.04
            fileending: ''

          - target: x86_64-apple-darwin
            runs-on: macos-26-intel
            fileending: ''

          - target: aarch64-apple-darwin
            runs-on: macos-26
            fileending: ''

          - target: x86_64-pc-windows-msvc
            runs-on: windows-latest
            fileending: '.exe'

          - target: armv7-unknown-linux-musleabi
            runs-on: ubuntu-24.04
            fileending: ''
    uses: ./.github/workflows/build-and-test-template.yml
    with:
      runs-on: ${{ matrix.runs-on }}
      target: ${{ matrix.target }}
      fileending: ${{ matrix.fileending }}
      docs_example_exclude_regex: ${{ vars.DOCS_EXAMPLE_EXCLUDE_REGEX || '^documentation/docs/examples/(automatic_screen_power_control/raspberry|motion_detection/raspberry)\.yml$' }}
      docs_example_placeholder_values: ${{ vars.DOCS_EXAMPLE_PLACEHOLDER_VALUES || '{}' }}
    # If you might want to use a secret in the build or test job then you would need to pass secrets, inherit disables secret isolation
    secrets: inherit

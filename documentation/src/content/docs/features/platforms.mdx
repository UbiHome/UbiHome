---
title: 'Connectivity and Platforms'
---

import { ShowcaseText } from 'starlight-showcases';
import { connectivityEntries } from './connectivityEntries';
import { platformEntries } from './platformEntries';

## Connectivity

Connect with Home Assistant via the native API, use MQTT or the built-in web server (REST API).

<ShowcaseText entries={connectivityEntries} />

## Platforms

Platforms implement how [components](/features/) interact with the system, for example reading device sensors or running shell commands. Use one of the already implemented platforms below, or create your own.

<ShowcaseText entries={platformEntries} />
